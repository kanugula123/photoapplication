import { CustomvalidatorsDirective } from './customvalidators.directive';

describe('CustomvalidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomvalidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
